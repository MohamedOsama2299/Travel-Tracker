# SQL Basics Definitions

## SELECT
Used to retrieve data from a database table.

## FROM
Specifies the table from which data will be retrieved.

## WHERE
Used to filter records by applying a condition.

## LIKE
Used to search for a pattern inside text values, usually with %.

## NOT NULL
Ensures that a column cannot contain empty values.

## INSERT DATA
Used to add new records into a database table.